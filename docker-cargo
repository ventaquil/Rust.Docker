#!/usr/bin/env bash

function handler {
    exit
}

trap handler INT

docker run --rm --user "$(id -u)":"$(id -g)" -it -v $(pwd)/cargo:/root/.cargo -v $(pwd)/application:/application -w /application rust cargo $@
